node_modules
dist
*.tsbuildinfo
.DS_Store
.env
.env.*
!.env.example
WORKING.md

# A vault's data directory. Running the app against `examples/vault/` creates
# one; the writer's own vaults are outside this repository.
.inkling/

apps/desk/src-tauri/target
apps/desk/src-tauri/gen

# Platform icon sets tauri generates but a macOS-only app never bundles.
apps/desk/src-tauri/icons/ios
apps/desk/src-tauri/icons/android
